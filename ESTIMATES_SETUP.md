# 土木見積もり機能 セットアップ

このドキュメントは「見積もり」機能を有効化するための初期設定手順です。

## 1. Supabase スキーマの適用

Supabase ダッシュボード → SQL Editor で **以下の順番で** 実行してください。

### 1-A. 前提テーブル（companies / users）の作成

`admin-schema.sql` と `companies-schema.sql` をまだ適用していない場合、または「`public.companies` が存在しません」エラーが出る場合は、まずこれを実行：

```text
supabase/companies-bootstrap.sql
```

このスクリプトは冪等です（既に適用済みでも安全に再実行できます）。次を作成・更新します：

- `public.set_updated_at()` 関数
- `public.users` テーブル
- `public.companies` テーブル
- `users.company_id` カラム
- `handle_new_user()` トリガー（auth.users 作成時に同期）
- companies / users の RLS ポリシー

### 1-B. 見積もり用スキーマ

続けて以下を実行：

```text
supabase/estimates-schema.sql
```

これで以下が作成されます：

- `estimates` テーブル（案件）
- `estimate_items` テーブル（見積もり明細）
- `estimate_messages` テーブル（チャット履歴）
- ストレージバケット `estimates`（設計図ファイル用）
- RLS ポリシー一式

### 1-C. 共有ドキュメント機能（**重要**）

続けて以下を実行：

```text
supabase/shared-documents-schema.sql
```

これで `documents` テーブルに `is_shared` カラムが追加され、フラグの立った資料は **全ユーザー横断で参照可能** になります。これにより単価表・歩掛資料は **一度 seed すれば、新規ユーザーが追加されても自動的にベクトル検索で参照される** ようになります。

## 2. 単価・歩掛資料を社内ナレッジに登録（**1回だけ**）

プロジェクト直下の `見積もり用資料/` フォルダにある PDF（国交省の労務単価・資材単価・共通仕様など）を、**共有ドキュメント**として `documents` テーブルに登録します。`is_shared = true` で登録されるため、新規ユーザー追加時に再登録は不要です。

### 一括登録

1. アプリを起動した状態で、ログイン済みのブラウザのコンソールから以下を実行：

```js
await fetch('/api/admin/seed-estimate-references', { method: 'POST' }).then(r => r.json())
```

または、curl で：

```bash
# ブラウザの DevTools → Application → Cookies からセッションcookieをコピーし、
# 環境変数 COOKIE に貼り付け
curl -X POST http://localhost:3000/api/admin/seed-estimate-references \
  -H "Cookie: $COOKIE"
```

2. レスポンスで各ファイルの状態（`ok` / `skipped` / `error`）が確認できます。
3. 同名・同カテゴリ・`is_shared = true` が既に登録されていれば自動でスキップされます。
4. **新規ユーザー追加時にこの seed を再実行する必要はありません。**

### 手動アップロード（任意）

通常の「ファイル追加」ページから個別にアップロードすることもできます。その際は **カテゴリを「単価表・歩掛」** にしてください。

## 3. 動作確認

1. サイドバーから **「見積もり」** を開く
2. **「新しい見積もり」** をクリック
3. 工事名・都道府県・工事区分を入力
4. 設計図ファイル（PDF / 画像 / Excel / DXF 等）をドロップ
5. **「AIに見積もりを依頼する」** をクリック
6. AI が解析を行い、項目別の概算見積もりが表示される
7. AI から不明点質問があれば、わかる範囲で回答 → 自動再解析
8. 詳細画面の **「Excel出力」** で `xlsx` をダウンロード
9. 画面下部のチャット欄で「擁壁部分だけの見積もりは？」など追加質問可能

## 4. 精度を上げるには

- **過去の見積書・設計書** を「ファイル追加」で社内ナレッジに登録すると、AI がベクトル検索で参照し精度が上がります
- **地域**（都道府県）を正しく設定すると、地域別労務単価が反映されます
- **不明点質問** に丁寧に答えるほど精度が上がります

## 注意事項

- 本機能で算出される金額は **概算（±20〜30%目安）** です。最終見積もりには現場確認・積算ソフトでの精算が必要です
- DXF / DWG はテキスト表現のみ解析されます。寸法の自動拾い精度は限定的です。重要な寸法は不明点質問で確認してください
